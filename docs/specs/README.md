# Specs Workspace

Only distributable references should be committed to this directory.

- Use `fetch-specs.sh` to populate redistributable references.
- Place non-redistributable references in `.local/` after obtaining them yourself.
- Keep citation notes in the relevant architecture/task documents instead of copying large proprietary excerpts.
