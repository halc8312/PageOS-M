# Responsive Behavior

- `>= 768px`: desktop mode with floating windows, dock, and status bar
- `< 768px`: mobile home with card launcher and fullscreen app canvas
- input unifies pointer, mouse, keyboard, and touch events into a common event stream
- component sizing uses fluid clamps to preserve readability on phones and tablets
